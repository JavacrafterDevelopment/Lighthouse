# Build output
bin/
obj/
dist/

# Generated by tools/make-icon.ps1
src/Lighthouse/lighthouse.ico

# WebView2 browser profile (cache, cookies, local storage). Created next to the
# exe at runtime so the app stays portable — never commit it. The bare `data/`
# matches it wherever the app is run from.
data/

# Runtime output, in case anything is ever written beside the exe
*.log
*.dmp

.vs/
.idea/
*.user
